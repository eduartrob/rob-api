name: Deploy to EC2 on develop branch

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2 instance
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Define el directorio de la aplicación
            APP_DIR="/home/${{ secrets.EC2_USER }}/rob-api"

            # Navega al directorio. Si no existe, clona el repositorio. Si existe, actualízalo.
            if [ -d "$APP_DIR" ]; then
              cd $APP_DIR
              echo ">>> Actualizando el repositorio..."
              git pull origin develop
            else
              echo ">>> Clonando el repositorio por primera vez..."
              git clone -b develop https://github.com/eduartrob/rob-api.git $APP_DIR
              cd $APP_DIR
            fi

            echo ">>> Reconstruyendo la imagen de la API y reiniciando el contenedor..."
            # El comando 'up' con '--build' reconstruirá la imagen 'api' si el código ha cambiado.
            # No afectará a Nginx, ya que se ejecuta en el host.
            docker compose up --build -d